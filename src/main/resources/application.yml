spring:
  application:
    name: birji-wala-demo
  datasource:
    url: jdbc:postgresql://192.168.1.229:5432/postgres
    username: postgresuser
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
    port: 8080
